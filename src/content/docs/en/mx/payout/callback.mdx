---
title: 代付回调
description: 商户接受一个代付结果的回调
---

import ReqAddr from "@components/ReqAddr.astro";
import Table from "@components/Table.astro";

### 回调地址

<ReqAddr method="POST" url="商户提供的回调地址" />

### 回调参数

<Table
  thead={["字段", "类型", "必需", "描述"]}
  tbody={[
    ["merchantOrderNo", "String", "yes", "商户订单号(Max leng 32)"],
    ["tradeNo", "String", "yes", "平台订单号"],
    ["remark", "String", "yes", "商户创建订单传的值"],
    ["status", "Int", "yes", "2-代付成功 3-代付失败 4-退款"],
    ["sign", "String", "yes", "签名"],
  ]}
/>

```json title=回调示例
{
  "merchantOrderNo": "201806251011",
  "tradeNo": "TF201806251011",
  "remark": "代付备注",
  "status": 2,
  "sign": "TEEMO_SIGN"
}
```

### 回调返回

<Table
  thead={["字段", "类型", "必需", "描述"]}
  tbody={[["SUCCESS", "String", "yes", '必须返回"SUCCESS"否则会重复回调']]}
/>

```json title=回调示例
{
  "SUCCESS": "SUCCESS"
}
```
